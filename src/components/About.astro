<section
  id="about"
  class="section absolute inset-0 opacity-0 pointer-events-none"
>
  <div class="about-frame">
    <div class="about-content">
      <p class="about-bio">Based in Burlington, Ontario. I build infrastructure that runs quietly and AI systems that make it smarter. I've been doing this long enough to know that the best solutions are the ones nobody notices.</p>

      <dl class="about-env">
        <div class="env-row"><dt>Editor</dt><dd>Neovim</dd></div>
        <div class="env-row"><dt>Terminal</dt><dd>Ghostty</dd></div>
        <div class="env-row"><dt>Shell</dt><dd>zsh</dd></div>
        <div class="env-row"><dt>Primary</dt><dd>macOS 26 Tahoe</dd></div>
        <div class="env-row"><dt>Secondary</dt><dd>Arch Linux / Niri</dd></div>
      </dl>

      <a
        class="about-dotfiles"
        href="https://github.com/Marcus-TG/dotfiles"
        target="_blank"
        rel="noreferrer"
      >View dotfiles →</a>
    </div>
  </div>
</section>

<style>
  .about-frame {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    padding: 9.2rem 3rem 2.5rem 3rem;
  }

  .about-content {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding-left: 42%;
    padding-right: 4%;
    gap: 2.5rem;
    /* Bias the stack slightly above the vertical center */
    transform: translateY(-4%);
  }

  .about-bio {
    max-width: 480px;
    font-weight: 300;
    font-size: 0.95rem;
    line-height: 1.6;
    color: var(--color-text-primary);
    opacity: 0.5;
    margin: 0;
    text-shadow:
      0 0 10px var(--color-background),
      0 0 4px var(--color-background);
  }

  .about-env {
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .env-row {
    display: grid;
    grid-template-columns: 7rem 1fr;
    align-items: baseline;
  }

  .env-row dt,
  .env-row dd {
    font-family: ui-monospace, SFMono-Regular, 'SF Mono', Menlo, Consolas,
      'Liberation Mono', monospace;
    font-size: 0.8rem;
    letter-spacing: 0.02em;
    color: var(--color-text-primary);
    margin: 0;
    text-shadow:
      0 0 10px var(--color-background),
      0 0 4px var(--color-background);
  }

  .env-row dt {
    opacity: 0.4;
  }

  .env-row dd {
    opacity: 1;
  }

  .about-dotfiles {
    font-family: ui-monospace, SFMono-Regular, 'SF Mono', Menlo, Consolas,
      'Liberation Mono', monospace;
    font-size: 0.8rem;
    letter-spacing: 0.02em;
    color: var(--color-accent-primary);
    text-decoration: none;
    opacity: 0.85;
    transition: opacity 0.2s ease;
    align-self: flex-start;
    text-shadow:
      0 0 10px var(--color-background),
      0 0 4px var(--color-background);
  }

  .about-dotfiles:hover {
    opacity: 1;
  }

  /* ── Mobile (≤768px) ── */
  @media (max-width: 768px) {
    .about-frame {
      padding: 7rem 1rem 1.5rem 0.5rem;
      overflow-y: auto;
      -webkit-overflow-scrolling: touch;
    }

    .about-content {
      padding-left: 0;
      padding-right: 1rem;
      justify-content: flex-start;
      gap: 2rem;
      transform: none;
    }

    .about-bio {
      font-size: 0.8rem;
      max-width: 100%;
    }

    .env-row {
      grid-template-columns: 6rem 1fr;
    }

    .env-row dt,
    .env-row dd {
      font-size: 0.7rem;
    }

    .about-dotfiles {
      font-size: 0.7rem;
    }
  }
</style>
